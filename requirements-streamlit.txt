# Minimal requirements for Streamlit deployment
# This is optimized for running the Streamlit dashboard
# All data is pre-computed and loaded from CSV files

streamlit==1.52.2
pandas>=2.0.0
numpy>=2.0.0
plotly>=6.0.0
